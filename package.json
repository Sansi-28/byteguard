{
  "name": "byteguard-pqc-platform",
  "version": "3.0.0",
  "description": "ByteGuard — Post-Quantum Secure Data Sharing Platform",
  "private": true,
  "scripts": {
    "dev:frontend": "cd client && npm run dev",
    "dev:backend": "cd backend && python app.py",
    "build": "cd client && npm run build",
    "install-all": "cd client && npm install && cd ../backend && pip install -r requirements.txt",
    "docker:up": "docker-compose up --build -d",
    "docker:down": "docker-compose down"
  },
  "license": "MIT"
}
